export function Footer() {
  return (
    <footer className="pb-2 text-center text-xs font-semibold text-[var(--game-muted)]">
      Vibe Arcade runs locally, saves in your browser, and makes no API calls.
    </footer>
  );
}

